# 蜗牛快搜 (D2D) 插件开发指南

> 2026-09-19 v3 (ABI 3)。插件 = `plugins\<插件id>\manifest.json`(声明) + `plugins\<插件id>\*.dll`(代码,可缺省)。
> 插件作者唯一需要的 SDK 头 = 仓库根 `xjs_plugin_sdk.h`;本指南完整描述 D2D 原生插件形态的全部约定。
> **v2 变更**:订阅事件不再携带 JSON 数据载荷,`OnEvent` 只传 (事件类型, 窗口令牌, 结果对象);
> 宿主表删除 `SelectionJson` — 选中集/结果集/路径一律经 `xunjieso.h` 直连自取,宿主不打包数据。
> **v3 变更**:命令/动态菜单/预览接管/搜索模式/输入拦截回调全部 C 化;文件上下文传**引擎 FileId 数组**
> (不再传 paths JSON),预览只传 fileId,宿主表 `OpenPath` → `OpenFile`(按 fileId 打开) —
> 路径/名称/扩展名一律 `xjs_db_GetPath`/`xjs_db_GetName` 直连引擎自取,宿主与插件间不传路径。

---

## 1. 最小可用插件

```
SnailQuickSearch.exe 所在目录\
  plugins\
    my-plugin\
      manifest.json        ← 必填
      my-plugin.dll        ← 有代码才需要 (manifest "动态库" 字段指名)
      data\                ← 宿主代管的插件私有存储 (StorageGet/Set 落这里, 自动创建)
```

- **id 必须与目录名一致**,限定 `[a-z0-9-]{1,48}`。
- **纯声明式插件**(无 `动态库` 字段)只能用模板类能力(`搜索模式` 带模板 / `托管搜索`),零代码可用。
- 目录不存在 = 无插件;新放进目录的插件在 设置-插件-重新扫描 后出现;**默认禁用**,首次启用弹风险确认框。

## 2. manifest.json 字段 (中文主键, 与主程序配置文件口径一致; 枚举值为英文数据串)

```json
{
  "标识": "my-plugin",
  "名称": "我的插件",
  "版本": "1.0.0",
  "类型": "library",            /* "library"(进程内) | "app"(自建窗口); 未知值 = 清单错误 */
  "动态库": "my-plugin.dll",    /* 相对插件目录; 缺省 = 纯声明式 */
  "接口版本": 3,                /* dll 存在时必填, 必须 = SDK 头 XJS_PLUGIN_ABI_VERSION (当前 3) */
  "作者": "…", "简介": "…", "图标": "icon.png",
  "权限": ["file.read", "file.write", "exec", "ui"],
  "能力": ["fileContextMenu", "searchBoxMenu", "searchModes", "hostedSearch",
            "searchInputIntercept", "statusBar", "events", "preview", "batchRename"],
  "菜单":       [ { "标识": "cmd1", "文字": "菜单项", "顺序": 10, "时机": "file", "扩展名": ["jpg", "png"] } ],
  "搜索框菜单": [ { "标识": "cmd2", "文字": "搜索框右键项", "顺序": 0 } ],
  "状态栏":     [ { "标识": "btn", "文字": "按钮", "提示": "悬停提示", "顺序": 10 } ],
  "搜索模式":   [ { "标识": "m1", "名称": "模式名", "简介": "简介", "类型": "sql",
                    "模板": "SELECT * FROM alltable WHERE FName LIKE '%<keyword>%'" } ],
  "托管搜索":   [ { "标识": "h1", "词条": ["暗号"], "模式": "regex", "查询": "^\\d+\\.jpg$" } ],
  "预览":       [ { "扩展名": ["docx", "xlsx"] } ]
}
```

| 字段 | 说明 |
|---|---|
| `菜单.时机` | `any`(默认) / `file` / `dir` / `drive`;目录/驱动器不匹配 `扩展名` |
| `菜单.扩展名` | 扩展名数组(不含点,忽略大小写);`"*"` = 全部;多选时所有文件都要命中 |
| `搜索模式` | **有 `模板`** = 宿主按模板执行(`<keyword>` 占位符 = 搜索框输入,零代码);**无 `模板`** = 接管型,输入命中/菜单点击时回调 `XjsPlugin_OnSearchMode` 由插件驱动 |
| `预览.扩展名` | 接管的扩展名(小写);`dir`/`drive` 伪扩展名接管文件夹/驱动器 |

## 3. 权限 (四档)

| 权限 | 授予的宿主 API |
|---|---|
| `file.read` | PathInfoJson / ReadFile / ListDirJson / ClipboardGetText |
| `file.write` | WriteFile / DeleteToRecycle / MoveCopyJson / RenameTo / ClipboardSetText |
| `exec` | ExecJson |
| `ui` | SearchSetText / OpenFile / ShowMainWindow |

**免权限**:Toast / Summon / GetSkinJson / DialogJson / Subscribe / Storage\* / Log(照引擎宿主"UI 操作/通知/存储免权限"口径)。
未声明就调用 = `ERR_PERM`。**诚实口径(必须写进你插件的用户须知)**:原生插件与主程序同权限运行(主程序以管理员运行);
权限 = 声明的意图 + 宿主 API 边界闸门,不是沙箱;**直连引擎不经过权限闸**(物理上拦不住)。

## 4. 生命周期与线程契约

- 固定导出三个:`XjsPlugin_GetInfo`(id 必须与清单/目录一致) / `XjsPlugin_Init(ctx, host)` / `XjsPlugin_Shutdown(ctx)`。
  能力回调按需导出:`OnCommand` / `BuildMenu` / `OnSearchMode` / `OnInput` / `OnPreview` / `OnEvent` / `OnHostGone`。
- DLL **一经加载不卸载**;禁用 = 宿主闸门(API 返回 `ERR_PERM`),菜单下次构建自然消失;替换 DLL 需重启。
- **宿主→插件回调只在 UI 线程**、绝不在绘制帧内;重活自己开线程并在 `Shutdown` 里 join。
- 插件→宿主:文件/存储/Log/Subscribe 任意线程;界面/对话框/Toast/GetSkinJson 仅 UI 线程,错线程返回 `ERR_THREAD`。
- 字符串一律 UTF-8;输出 = 调用方缓冲(`buf==NULL` 取所需长度);结构体只追加;异常禁止越过边界。

## 5. 直连搜索引擎 (本 SDK 特有, 比经宿主封装更灵活)

```cpp
#include "xunjieso.h"      // 与 xjs_plugin_sdk.h 同在仓库根
#pragma comment(lib, "xunjieso.lib")   // 或 build.bat 里显式链接

xjs_engine* eng = xjs_GetDefaultEngine();          // 宿主进程内同一 xunjieso.dll 实例
int state = xjs_db_GetEngineState(eng);            // 0空闲 1加载 2保存 3扫描 4同步 5搜索
if (state == 0) {
    xjs_result* r = xjs_result_Create(eng);        // 引擎空闲时才创建结果对象
    int fp = xjs_result_Query(r, sql, XJS_KEYWORD_SQL, FALSE);
}
```

**注意**:库未就绪(加载/扫描中)时创建的结果对象会被永久定成文件名序 — 先查 `xjs_db_GetEngineState`。
`xjs_lua_RegisterFunction` 可向引擎注册自定义 Lua C 函数,供 Lua 搜索模式调用。

## 6. 命令与回调 (文件引用一律引擎 FileId, 路径直连引擎自取)

| 回调 | 参数 |
|---|---|
| `OnCommand` | `void OnCommand(ctx, const char* cmdIdUtf8, XjsWindowToken window, const int* fileIds, int count, int kind)` — cmdIdUtf8 = 清单 "标识"; fileIds = 文件上下文 (引擎 FileId 数组, 调用期间有效, 只读); kind = `XJS_PLUGIN_CTX_NONE/FILE/DIR/DRIVE`(状态栏项/搜索框命令 = NONE, fileIds=NULL) |
| `BuildMenu` 返回 | `[{"标识":"..","文字":"..","顺序":0},..]`(与清单 `菜单` 项同构,中文键);入参 `(ctx, kindUtf8, window, fileIds, count, inputUtf8, buf, cap)` — "fileCtx" 带 fileIds(input=NULL),"searchBox" 带 inputUtf8(fileIds=NULL) |
| `OnSearchMode` | `(ctx, modeIdUtf8, window, inputUtf8)`;返回 1 = 已接管 |
| `OnInput` | `(ctx, window, inputUtf8)`;返回 1 = 拦截本次原生搜索(插件自己发起的搜索不再二次询问) |
| `OnPreview` | `(ctx, requestId, window, fileId)`;返回 1 = 已接管,异步调 `PreviewDeliverBitmap`(32bpp BGRA 预乘 alpha, stride=字节行距) / `PreviewDeliverText`;世代过期宿主静默丢弃 |
| `OnEvent` | 纯信号,不传 JSON:`void OnEvent(ctx, int eventType, XjsWindowToken window, void* result)`;`eventType` = `XJS_PLUGIN_EVT_*`,`window` = 所属窗口令牌(进程级事件 = 0),`result` = 该窗口的引擎结果对象(即 `xjs_result*`,直连引擎的插件直接转型使用;进程级事件 = NULL)。选中数/选中集/结果数/路径等数据一律用引擎接口自取:`xjs_result_GetSelectedCount` / `xjs_result_CopySelectedFileId` / `xjs_result_GetCount` / `xjs_db_GetPath`…。宿主不打包任何数据(曾经的全选 paths JSON 在几百万结果下是秒级卡顿源,已废除) |

窗口令牌 `window` 只在宿主回调里产生,原样回传;`0` = 主窗缺省;失效 = `ERR_NOTFOUND`,**不得缓存/伪造**。
**文件引用口径 (v3)**:宿主与插件之间只传引擎 FileId,不传路径 — `xjs_db_GetPath`/`xjs_db_GetName`/`xjs_db_GetFileSize` 按 ID 自取;`OpenFile(window, fileId, reveal)` 走宿主打开行为(提权/异步/打开后隐藏, 按 ID 内部取路径);ReadFile/WriteFile/DeleteToRecycle 等通用文件操作仍按路径(操作插件自有的任意文件,不限于索引内)。

## 7. 形态取舍速查

- 插件形态 = C ABI DLL,UI 自理(示例用 GDI+,与宿主 D2D 零耦合);宿主交互走 `host->` 函数表。
- 权限四档(`ui` 档管界面类 API);引擎直连不受权限闸,插件自理引擎侧门槛。
- 重扫手动触发,DLL 替换需重启;不做在线商店,手动放目录;无 JS 宿主 — 直接写本形态插件即可。

完整可编译示例:`插件示例\space-map\`(右键驱动器/文件夹 → 空间地图, 状态栏"空间地图"入口;GDI+ 矩形树图 + 引擎直连 + ui 权限)。
