# 蜗牛快搜 (D2D) 插件开发指南

> 2026-09-22 v4 (ABI 4)。插件 = `plugins\<插件id>\manifest.json`(声明) + `plugins\<插件id>\*.dll`(代码,可缺省)。
> 插件作者唯一需要的 SDK 头 = 仓库根 `xjs_plugin_sdk.h`;本指南完整描述 D2D 原生插件形态的全部约定。
> **v2 变更**:订阅事件不再携带 JSON 数据载荷,`OnEvent` 只传 (事件类型, 窗口令牌, 结果对象);
> 宿主表删除 `SelectionJson` — 选中集/结果集/路径一律经 `xunjieso.h` 直连自取,宿主不打包数据。
> **v3 变更**:命令/动态菜单/预览接管/搜索模式/输入拦截回调全部 C 化;文件上下文传**引擎 FileId 数组**
> (不再传 paths JSON),预览只传 fileId,宿主表 `OpenPath` → `OpenFile`(按 fileId 打开) —
> 路径/名称/扩展名一律 `xjs_db_GetPath`/`xjs_db_GetName` 直连引擎自取,宿主与插件间不传路径。
> **v4 变更**:新增**面板接管**能力 (清单 `能力: ["preview-panel"]`) — 插件整块接管预览面板体
> (含头部带),宿主转发 鼠标/滚轮/键盘/IME (`XjsPlugin_OnPanelEvent`),插件交付整块 BGRA 位图
> (宿主表追加 6 个 `Panel*` 函数指针,取用前先校验 `host->size`,旧宿主跑新插件 = 干净失败)。
> 交互式面板 (AI 助手等) 用它;静态预览内容仍用 v3 的 `preview` 能力。首例实现见 `插件示例\ai-assistant`。
> **v4 表尾追加 (2026-09-24)**:宿主表新增 `QueryApi(name)` 名称解析入口,**宿主表自此冻结** —
> 之后新增宿主能力(设置读写/界面操作/搜索模式管理等,见第 9 节)一律"加名字 + 加类型",不再扩表不再动 ABI 号;
> 插件取用前照例校验 `host->size >= offsetof(XjsPluginHost, QueryApi) + sizeof(void*)`。
> 权限同步加第五档 `settings`(设置写入/运行时搜索模式管理)。

---

## 1. 最小可用插件

```
SnailQuickSearch.exe 所在目录\
  plugins\
    my-plugin\
      manifest.json        ← 必填
      my-plugin.dll        ← 有代码才需要 (manifest "动态库" 字段指名)
      data\                ← 宿主代管的插件私有存储 (StorageGet/Set 落这里, 自动创建)
```

- **id 必须与目录名一致**,限定 `[a-z0-9-]{1,48}`。
- **纯声明式插件**(无 `动态库` 字段)只能用模板类能力(`搜索模式` 带模板 / `托管搜索`),零代码可用。
- 目录不存在 = 无插件;新放进目录的插件在 设置-插件-重新扫描 后出现;**默认禁用**,首次启用弹风险确认框。

## 2. manifest.json 字段 (中文主键, 与主程序配置文件口径一致; 枚举值为英文数据串)

```json
{
  "标识": "my-plugin",
  "名称": "我的插件",
  "版本": "1.0.0",
  "类型": "library",            /* "library"(进程内) | "app"(自建窗口); 未知值 = 清单错误 */
  "动态库": "my-plugin.dll",    /* 相对插件目录; 缺省 = 纯声明式 */
  "接口版本": 4,                /* dll 存在时必填, 必须 = SDK 头 XJS_PLUGIN_ABI_VERSION (当前 4) */
  "作者": "…", "简介": "…", "图标": "icon.png",
  "权限": ["file.read", "file.write", "exec", "ui"],
  "能力": ["fileContextMenu", "searchBoxMenu", "searchModes", "hostedSearch",
            "searchInputIntercept", "statusBar", "events", "preview", "batchRename",
            "preview-panel"],
  "菜单":       [ { "标识": "cmd1", "文字": "菜单项", "顺序": 10, "时机": "file", "扩展名": ["jpg", "png"] } ],
  "搜索框菜单": [ { "标识": "cmd2", "文字": "搜索框右键项", "顺序": 0 } ],
  "状态栏":     [ { "标识": "btn", "文字": "按钮", "提示": "悬停提示", "顺序": 10 } ],
  "搜索模式":   [ { "标识": "m1", "名称": "模式名", "简介": "简介", "类型": "sql",
                    "模板": "SELECT * FROM alltable WHERE FName LIKE '%<keyword>%'" } ],
  "托管搜索":   [ { "标识": "h1", "词条": ["暗号"], "模式": "regex", "查询": "^\\d+\\.jpg$" } ],
  "预览":       [ { "扩展名": ["docx", "xlsx"] } ]
}
```

| 字段 | 说明 |
|---|---|
| `菜单.时机` | `any`(默认) / `file` / `dir` / `drive`;目录/驱动器不匹配 `扩展名` |
| `菜单.扩展名` | 扩展名数组(不含点,忽略大小写);`"*"` = 全部;多选时所有文件都要命中 |
| `搜索模式` | **有 `模板`** = 宿主按模板执行(`<keyword>` 占位符 = 搜索框输入,零代码);**无 `模板`** = 接管型,输入命中/菜单点击时回调 `XjsPlugin_OnSearchMode` 由插件驱动 |
| `预览.扩展名` | 接管的扩展名(小写);`dir`/`drive` 伪扩展名接管文件夹/驱动器 |

## 3. 权限 (四档)

| 权限 | 授予的宿主 API |
|---|---|
| `file.read` | PathInfoJson / ReadFile / ListDirJson / ClipboardGetText |
| `file.write` | WriteFile / DeleteToRecycle / MoveCopyJson / RenameTo / ClipboardSetText |
| `exec` | ExecJson |
| `ui` | SearchSetText / OpenFile / ShowMainWindow / window.cmd / window.create / modes.apply |
| `settings` | settings.set / settings.global.set / modes.add / modes.remove (扩展 API, 见第 9 节) |

**免权限**:Toast / Summon / GetSkinJson / GetSkinJsonOf / DialogJson / Subscribe / Storage\* / Log(照引擎宿主"UI 操作/通知/存储免权限"口径);扩展 API 的读面同样免权限(settings.get / settings.global.get / windows.enum / window.state / modes.list)。
未声明就调用 = `ERR_PERM`。**诚实口径(必须写进你插件的用户须知)**:原生插件与主程序同权限运行(主程序以管理员运行);
权限 = 声明的意图 + 宿主 API 边界闸门,不是沙箱;**直连引擎不经过权限闸**(物理上拦不住)。
自建窗口跟随皮肤:`GetSkinJsonOf(window, buf, cap)` 取指定窗口的色板(window=0 = 默认窗口;与 `Panel*` 同为宿主表追加指针,取用前先校验 `host->size`),再订阅 `XJS_PLUGIN_EVT_SKIN`(换肤事件,window = 换肤窗口令牌)在皮肤变化后重取重绘 — 参照插件示例 space-map。

## 4. 生命周期与线程契约

- 固定导出三个:`XjsPlugin_GetInfo`(id 必须与清单/目录一致) / `XjsPlugin_Init(ctx, host)` / `XjsPlugin_Shutdown(ctx)`。
  能力回调按需导出:`OnCommand` / `BuildMenu` / `OnSearchMode` / `OnInput` / `OnPreview` / `OnEvent` / `OnPanelEvent` /
  `OnPluginMessage`(插件间消息收信口, 见第 10 节) / `OnHostGone`。
- DLL **一经加载不卸载**;禁用 = 宿主闸门(API 返回 `ERR_PERM`),菜单下次构建自然消失;替换 DLL 需重启。
- **宿主→插件回调只在 UI 线程**、绝不在绘制帧内;重活自己开线程并在 `Shutdown` 里 join。
- 插件→宿主:文件/存储/Log/Subscribe 任意线程;界面/对话框/Toast/GetSkinJson/GetSkinJsonOf 仅 UI 线程,错线程返回 `ERR_THREAD`。
- 字符串一律 UTF-8;输出 = 调用方缓冲(`buf==NULL` 取所需长度);结构体只追加;异常禁止越过边界。

## 5. 直连搜索引擎 (本 SDK 特有, 比经宿主封装更灵活)

```cpp
#include "xunjieso.h"      // 与 xjs_plugin_sdk.h 同在仓库根
#pragma comment(lib, "xunjieso.lib")   // 或 build.bat 里显式链接

xjs_engine* eng = xjs_GetDefaultEngine();          // 宿主进程内同一 xunjieso.dll 实例
int state = xjs_db_GetEngineState(eng);            // 0空闲 1加载 2保存 3扫描 4同步 5搜索
if (state == 0) {
    xjs_result* r = xjs_result_Create(eng);        // 引擎空闲时才创建结果对象
    int fp = xjs_result_Query(r, sql, XJS_KEYWORD_SQL, FALSE);
}
```

**注意**:库未就绪(加载/扫描中)时创建的结果对象会被永久定成文件名序 — 先查 `xjs_db_GetEngineState`。
`xjs_lua_RegisterFunction` 可向引擎注册自定义 Lua C 函数,供 Lua 搜索模式调用。

## 6. 命令与回调 (文件引用一律引擎 FileId, 路径直连引擎自取)

| 回调 | 参数 |
|---|---|
| `OnCommand` | `void OnCommand(ctx, const char* cmdIdUtf8, XjsWindowToken window, const int* fileIds, int count, int kind)` — cmdIdUtf8 = 清单 "标识"; fileIds = 文件上下文 (引擎 FileId 数组, 调用期间有效, 只读); kind = `XJS_PLUGIN_CTX_NONE/FILE/DIR/DRIVE`(状态栏项/搜索框命令 = NONE, fileIds=NULL) |
| `BuildMenu` 返回 | `[{"标识":"..","文字":"..","顺序":0},..]`(与清单 `菜单` 项同构,中文键);入参 `(ctx, kindUtf8, window, fileIds, count, inputUtf8, buf, cap)` — "fileCtx" 带 fileIds(input=NULL),"searchBox" 带 inputUtf8(fileIds=NULL) |
| `OnSearchMode` | `(ctx, modeIdUtf8, window, inputUtf8)`;返回 1 = 已接管 |
| `OnInput` | `(ctx, window, inputUtf8)`;返回 1 = 拦截本次原生搜索(插件自己发起的搜索不再二次询问) |
| `OnPreview` | `(ctx, requestId, window, fileId)`;返回 1 = 已接管,异步调 `PreviewDeliverBitmap`(32bpp BGRA 预乘 alpha, stride=字节行距) / `PreviewDeliverText`;世代过期宿主静默丢弃 |
| `OnEvent` | 纯信号,不传 JSON:`void OnEvent(ctx, int eventType, XjsWindowToken window, void* result)`;`eventType` = `XJS_PLUGIN_EVT_*`,`window` = 所属窗口令牌(进程级事件 = 0),`result` = 该窗口的引擎结果对象(即 `xjs_result*`,直连引擎的插件直接转型使用;进程级事件 = NULL)。选中数/选中集/结果数/路径等数据一律用引擎接口自取:`xjs_result_GetSelectedCount` / `xjs_result_CopySelectedFileId` / `xjs_result_GetCount` / `xjs_db_GetPath`…。宿主不打包任何数据(曾经的全选 paths JSON 在几百万结果下是秒级卡顿源,已废除) |
| `OnPanelEvent` (v4, preview-panel 能力) | `void OnPanelEvent(ctx, XjsWindowToken window, const XjsPanelEvent* ev)` — 见第 7 节 |

窗口令牌 `window` 只在宿主回调里产生,原样回传;`0` = 主窗缺省;失效 = `ERR_NOTFOUND`,**不得缓存/伪造**。
**文件引用口径 (v3)**:宿主与插件之间只传引擎 FileId,不传路径 — `xjs_db_GetPath`/`xjs_db_GetName`/`xjs_db_GetFileSize` 按 ID 自取;`OpenFile(window, fileId, reveal)` 走宿主打开行为(提权/异步/打开后隐藏, 按 ID 内部取路径);ReadFile/WriteFile/DeleteToRecycle 等通用文件操作仍按路径(操作插件自有的任意文件,不限于索引内)。
**SearchSetText 的 mode 参数 (2026-09-24 接线)**:`SearchSetText(window, kw, mode, execute)` 的 `mode` = `wildcard|regex|sql|lua` 切换该窗口搜索模式(与 settings.set `"搜索模式"` 同口径:非法名 = `ERR_ARG`,写入即落盘);`kw` + `execute=1` = 置入即搜,`execute=0` = 只填不搜(此时模式在下次搜索生效);`kw` 空且 `execute=1` = 只按当前(新)模式重搜现词。

## 7. 形态取舍速查

- 插件形态 = C ABI DLL,UI 自理(自绘示例 space-map 用 GDI+,与宿主 D2D 零耦合;整面板 UI 也可交给系统 WebView2 渲染,见 ai-assistant);宿主交互走 `host->` 函数表。
- 权限五档(`ui` 档管界面类 API,`settings` 档管扩展 API 的设置写入与模式管理);引擎直连不受权限闸,插件自理引擎侧门槛。
- 重扫手动触发,DLL 替换需重启;不做在线商店,手动放目录;无 JS 宿主 — 直接写本形态插件即可。

完整可编译示例:`插件示例\space-map\`(右键驱动器/文件夹 → 空间地图, 状态栏"空间地图"入口;GDI+ 矩形树图 + 引擎直连 + ui 权限)、`插件示例\ai-assistant\`(AI 助手,面板接管首例)。

## 9. 扩展 API (QueryApi 名称解析) — 读写程序设置 / 界面操作 / 搜索模式管理

宿主表尾有一个名称解析器 `host->QueryApi(name)`(2026-09-24 追加,**宿主表自此冻结**,以后新增能力一律加名字不加表):
传 API 名返回函数指针,未知名或旧宿主返回 NULL(干净降级)。返回的指针终身有效。全部**仅 UI 线程**(错线程 `ERR_THREAD`)。

```cpp
// Init 里:
if (host->size < offsetof(XjsPluginHost, QueryApi) + sizeof(void*))
    return XJS_PLUGIN_ERR_FAIL;                      // 旧宿主: 没有扩展 API, 按需降级
auto winEnum = (XjsApiWindowsEnum)host->QueryApi(ctx, XJS_API_WINDOWS_ENUM);
if (winEnum) { char j[1024]; winEnum(ctx, j, sizeof(j)); /* [{"令牌":1,"名称":"默认窗口","主窗":true,"档案槽":0}] */ }
```

| 名称常量 | 类型 | 权限 | 说明 |
|---|---|---|---|
| `XJS_API_SETTINGS_GET` | `XjsApiSettingsGet` | 免 | 每窗设置快照 JSON(中文键, 与配置键同名) |
| `XJS_API_SETTINGS_SET` | `XjsApiSettingsSet` | settings | 白名单键写入, 即时生效并落盘 |
| `XJS_API_GLOBAL_GET` / `_SET` | `XjsApiGlobalGet/Set` | 免 / settings | 全局项:`双击Ctrl目标`(空=禁用/"默认窗口"/档案名)、`绘制引擎`(d2d/gdiplus, 重启生效) |
| `XJS_API_WINDOWS_ENUM` | `XjsApiWindowsEnum` | 免 | `[{"令牌","名称","主窗","档案槽"}]` |
| `XJS_API_WINDOW_STATE` | `XjsApiWindowState` | 免 | 窗口运行态:视图/皮肤/缩放/搜索词/结果数/选中数/窗口矩形… |
| `XJS_API_WINDOW_CMD` | `XjsApiWindowCmd` | ui | `"show"` 唤起 / `"dismiss"` 窗口消失统一策略 / `"openSettings"` 打开设置窗并绑定该窗口 |
| `XJS_API_WINDOW_CREATE` | `XjsApiWindowCreate` | ui | 按档案名建窗(NULL=新空白档案; 档案已开=幂等激活); 回传令牌; 扫描中=ERR_STATE |
| `XJS_API_MODES_LIST` | `XjsApiModesList` | 免 | 该窗口可见模式:`[{"标识","名称","简介","类型","模板","来源":"用户|插件:<id>"}]` |
| `XJS_API_MODES_ADD` | `XjsApiModesAdd` | settings | 运行时添加模板型模式(见下), 回传 `{"标识":"p:<插件id>:<序>"}` |
| `XJS_API_MODES_REMOVE` | `XjsApiModesRemove` | settings | 只能删自己 add 的; 用户自定义/清单模式不归 API 管 |
| `XJS_API_MODES_APPLY` | `XjsApiModesApply` | ui | 语义=用户在药丸菜单点了这个模式:模板型转托管标签执行;接管型回调自己 OnSearchMode |

**settings.set 键表**(JSON 子集随意;全部键都要合法,一个未知键=ERR_ARG 整体拒绝,不做半套;写入即落盘):
`视图`("list/details/medium/large")、`页面缩放`(50..200)、`皮肤`(现存皮肤名)、`预览`(bool)、`预览宽度`(160..2000)、
`置顶`(bool)、`失焦行为`(0/1)、`显示控制按钮`/`显示筛选框`/`显示状态栏`/`任务栏图标`(bool)、`鼠标打开`(0=双击 1=单击)、
`默认选中`(0/1)、`搜索模式`("wildcard/regex/sql/lua",切换即按新模式重搜)。
例:`{"视图":"details","页面缩放":150,"置顶":true}`。

**运行时搜索模式(modes.add)是会话级的**:不写进配置文件,插件禁用或退出即消失 — 要持久模式用清单 `"搜索模式"` 声明。
添加后模式立即出现在搜索框药丸菜单并可被关键词命中转托管标签(与用户自定义模式同一执行链);输入命中词即模板执行,
`<keyword>` = 搜索框纯输入文字。每个插件上限 64 个。`modes.add` 需要清单 `能力` 含 `searchModes`。

```json
{ "名称": "只搜图片", "简介": "扩展名含图片的文件", "类型": "sql",
  "模板": "SELECT * FROM alltable WHERE FName LIKE '%<keyword>%' AND (FName LIKE '%.jpg' OR FName LIKE '%.png')" }
```

**诚实口径不变**:权限 = 声明的意图 + 宿主 API 边界闸门,不是沙箱;直连引擎不经权限闸。扩展 API 的设置写入走的是
用户在设置页走的同一批应用入口(改字段+落盘+重绘),不是旁路。

## 10. 插件互操作桥梁 (plugins.* / msg.*) — 判断同伴、给同伴发消息

全部**免权限、仅 UI 线程**,照旧经 `host->QueryApi` 解析。消息一律**经宿主中转**,插件之间不直连 —
启停闸门、发送方身份(`fromId` 由宿主填,不可伪造)、线程契约宿主统一把守。

**发现(判断"有没有某个插件 / 启用没有")**:
- `plugins.list` → 注册表扫到的**全部**插件(含禁用/清单错误项),按标识排序:
  `[{"标识":"..","名称":"..","版本":"..","作者":"..","简介":"..","类型":"library|app","启用":bool,"已加载":bool}]`
- `plugins.state(id)` → 没这个 id 也返回 OK:`{"存在":false}`;找到:`{"存在":true,"启用":bool,"已加载":bool,…同上元数据}`
  ("存在与否"是查询结果,不走错误码;要操作的目标不存在才返回 `ERR_NOTFOUND`)

**消息**:
- 收信 = **可选导出** `int XjsPlugin_OnPluginMessage(ctx, const char* fromIdUtf8, const char* jsonUtf8, char* buf, int cap)` —
  导出它才收得到消息(GetProcAddress 探测,缺失 = 收不到)。返回值 = 写入 buf 的回复字节数(调用方缓冲约定;
  0 = 无回复;负值 = 本插件向发送方报告的错误)。
- `msg.send(目标id, json, buf, cap)` 同步点对点:返回目标写的回复字节数(0 = 无回复,负值 = 对方报的错误)。
  目标没这个 id = `ERR_NOTFOUND`;目标禁用/未加载/没导出收信口 = `ERR_STATE`;A↔B 互发递归上限 16 层(超 = `ERR_STATE`)。
- `msg.broadcast(json)` 广播给全部"启用且已加载且导出收信口"的插件(不含自己),不收集回复(收信方 buf=NULL)。
- 载荷 UTF-8 ≤1MB,内容双方自定(建议 JSON,中文键与全库口径一致)。
- 想知道同伴上线/下线:Subscribe 加 `XJS_PLUGIN_EVT_PLUGINS`(位 1<<5)— 有插件启用/禁用/重扫后收到纯信号
  (进程级,window=0,result=NULL),收到后自己重查 `plugins.list`;**启动期不派发**,Init 时自己查一次即可。

```cpp
// 发送方:
auto send = (XjsApiMsgSend)host->QueryApi(ctx, XJS_API_MSG_SEND);
char reply[512];
int n = send ? send(ctx, "ai-assistant", "{\"动作\":\"查询会话\"}", reply, sizeof(reply)) : XJS_PLUGIN_ERR_STATE;
// n > 0: reply 里有 n 字节回复; n == 0: 受理无回复; n < 0: 错误码 (含对方报告的)

// 收信方 (导出):
int XJS_PLUGIN_CALL XjsPlugin_OnPluginMessage(XjsPluginCtx* ctx, const char* fromId,
                                              const char* json, char* buf, int cap) {
    if (!_stricmp(fromId, "ai-assistant")) { /* 同类间约定 */ }
    return 0;   // 无回复
}
```

**契约**:OnPluginMessage 恒 UI 线程 — 不要在里面等自己的工作线程(会卡住宿主 UI),回缓存值或先受理再查询;
工作线程产生的数据想发给同伴,先落自己的状态,在 UI 线程回调/定时点里再 `msg.send`。

## 8. 面板接管 (preview-panel 能力, v4) — 交互式面板的推荐形态

`preview` 能力只能交付**静态**位图/文本(没有交互回传),撑不起聊天窗、表格这类可交互 UI。
面板接管让插件**整块接管预览面板体**(含头部带,宿主不画头部;关闭入口 = 插件自绘 ✕,点击调
`PanelClose` 结束接管并按打开前状态恢复预览 — 预览本来开着只关面板,本来关着连预览一起关)。
列表不受影响。

**生命周期**:
- 插件(如响应状态栏项点击)调 `host->PanelOpen(ctx, window)` → 预览面板没开时宿主自动展开 →
  宿主回调 `OnPanelEvent(OPEN, serial, w, h, scale)`;已激活时幂等。
- 尺寸/缩放变化(窗口缩放、预览宽拖、页面缩放、DPI、状态栏显隐)→ 宿主 `serial++` 并回调
  `RESIZE`(新 w/h/scale)。插件自绘 ✕ 调 `PanelClose` / 窗口销毁 / 插件被禁用 → `CLOSE`。
- 插件主动收尾(即面板关闭按钮)调 `host->PanelClose(ctx, window)`。

**渲染模型**(UI 自理 = 插件自己画,宿主只贴位图):
1. 交互事件(`MOUSE_MOVE/LDOWN/LUP/RDOWN/RUP/DBLCLK/WHEEL/KEY_DOWN/KEY_CHAR/FOCUS/CAPTURE_LOST`,
   坐标 = 面板内容区内**物理像素**,`scale` = dpi×页面缩放,字号几何按它缩放)里更新自己的状态;
2. `host->PanelGetInfo(ctx, window, &serial, &w, &h, &scale)` 取当前世代(**任意线程**可调);
3. 渲染整块 32bpp BGRA(预乘 alpha、自上而下)位图,`host->PanelDeliverBitmap(ctx, window, serial,
   w, h, bgra, stride)` 交付(**任意线程**可调;serial/w/h 与当前世代不符 = 静默丢弃)。
   交付慢可先回自建线程再交付,但宿主回调线程仍是 UI 线程、禁在 WM_PAINT 内。

**键盘与 IME**(位图型面板):点击你的输入框后调 `PanelSetFocus(ctx, window, 1)` — 宿主把键盘/IME 让给面板
(此后 `KEY_DOWN/KEY_CHAR` 持续到达,字符含 IME 上屏,代理对拆两发);点击输入框以外调
`PanelSetFocus(ctx, window, 0)` 归还。光标移动时调 `PanelSetCaret(ctx, window, x, y)` 钉组字/
候选窗位置。`KEY_DOWN` 的 `delta` = 虚拟键码(`VK_PROCESSKEY` = IME 组合中,忽略)。

**真子窗口型面板(WebView2 等,2026-09-23 起 ai-assistant 的形态)**:自带完整输入体系的渲染层
不必走"宿主转发事件 + 交付位图"—— 用 v4 追加的 `host->PanelGetRect(ctx, window, &hwnd, &x, &y,
&w, &h)`(仅 UI 线程)取面板内容区矩形与所属搜索窗 HWND,在宿主窗内建自己的真子窗口盖住面板,
鼠标/键盘/IME/选区/滚动全部归子窗口原生体系;OPEN/RESIZE 时按新矩形重定位(缩放档 = scale ÷
GetDpiForWindow,WebView2 已按父窗 DPI 自动缩放),CLOSE 时收掉子窗口。OnPanelEvent 里除
OPEN/RESIZE/CLOSE 外的事件一律忽略即可;焦点进出子窗口时调 `PanelSetFocus` 同步宿主键盘路由
(搜索框双光标防线仍生效)。
**WebView2 回调引用语义(必踩坑,2026-09-23 实锤)**:创建完成回调
(`CreateCoreWebView2EnvironmentCompletedHandler` / `...ControllerCompletedHandler::Invoke`)
交付的 environment/controller 指针是**借用引用** — Invoke 一返回 WebView2 即释放,裸存指针
= 下一次虚表调用即 UAF 崩溃(表象:点开面板秒崩,崩溃点在下一个 `CreateCoreWebView2Controller`/
`put_Bounds` 的 vtable 解引用)。接受时必须 `AddRef()` 自留(官方样例用 QueryInterface 同义),
放弃分支只 `Close()` 不 Release;getter(`get_CoreWebView2` 等)自带引用可直存。加载器用官方
`WebView2Loader.dll` 动态加载(LoadLibrary+GetProcAddress,随 DLL 部署),不必静态链接。

**硬约束**:
- 宿主表 `Panel*` 指针是 v4 追加 — Init 里先校验 `host->size >= offsetof(XjsPluginHost,
  PanelSetCaret) + sizeof(void*)` 再用(用 `PanelGetRect` 再加一个指针),不够 = 返回 Init 失败(旧宿主干净降级)。
- `OnPanelEvent` 必须导出,清单 `能力` 含 `preview-panel`,`PanelOpen` 才会被放行。
- 禁用/重扫导致 owner 失效时宿主立即结束会话(回调 `CLOSE`) — 别缓存窗口令牌以外的宿主状态。
- 完整首例:`插件示例\ai-assistant`(状态栏"AI 助手" → 预览面板聊天;**整套 UI = 系统 WebView2**
  (Edge 运行时,系统组件)渲染:插件在面板内容区建真子窗口,WebView2 控制器以其为父,单文件
  HTML+CSS+JS 前端内嵌 DLL (`ai_web_ui.cpp`,NavigateToString 装载 + CSP 关 fetch/外域),
  C++↔JS 走 PostWebMessage JSON 桥 (`ai_web.cpp`: 环境懒建/子窗定位/焦点同步/命令分发),
  markdown 由 md4c 转 HTML 后前端注入,模型输出永不产生活 HTML;皮肤色经 GetSkinJson 派生
  成 CSS 变量推送,换肤订阅 EVT_SKIN 重推;WinHTTP SSE
  流式,OpenAI Responses 协议 `/responses`,多对话历史存 Storage;**agent 工具循环**:Responses
  function calling — 模型多轮调用 `run_search`/`open_file`/`copy_paths` 工具,插件在工作线程直连
  引擎在**私有结果对象**上执行搜索(`xjs_result_Query`/`xjs_result_ExecuteLua` 的 `waitComplete=TRUE`
  即阻塞等完成,主线程被引擎拒绝,工作线程正是推荐用法),工具结果 JSON 回填对话继续推理;
  系统提示词 = 角色说明 + `xjs_LUA_GetPprompt(0/1)` 引擎内嵌的 Lua 过滤/执行两份规范)。
  源码按职责分文件(共享头 `ai_assistant.h` 声明全部跨文件符号):`ai_core.cpp` 基础设施
  (编码/JSON/配置/历史)、`ai_agent.cpp` agent 大脑(引擎工具/提示词/SSE 轮/工作线程)、
  `ai_session.cpp` 会话层(会话池/发送/流泵/消息窗口)、`ai_web.cpp` Web 前端宿主(WebView2 生命周期/
  JSON 桥/调色派生/markdown→HTML)、`ai_web_ui.cpp` 嵌入式前端(整套对话 UI 的 HTML+CSS+JS)、
  `ai_plugin.cpp` 插件边界(导出面)。
