# <img src="snailsearch.ico" width="28" alt=""> 蜗牛快搜 SnailQuickSearch

**Windows 本地文件即时搜索工具** —— 全盘索引、输入即搜、秒级定位,常驻内存占更低。纯 Direct2D 自绘界面,内置可编程插件系统。

[![Platform](https://img.shields.io/badge/platform-Windows%2010%2F11-blue)](#-系统要求)
[![Language](https://img.shields.io/badge/C%2B%2B-20-00599C?logo=cplusplus&logoColor=white)](#-从源码构建)
[![UI](https://img.shields.io/badge/render-Direct2D%20%2F%20GDI%2B-0078D4)](#-界面与交互)


---

## ✨ 特性一览

- **输入即搜** —— 打字的同时出结果,无防抖延迟;过期查询由引擎自动丢弃。
- **全盘索引** —— 基于索引库的文件名/路径/大小/时间/属性检索,数百万文件规模依然流畅。
- **内存占用极低** —— 同规模全盘索引下常驻内存低于 Everything 1.5 与 Listary v7:451 万文件实测,专用工作集 281 MB vs 377 MB / 395 MB(详见下表)。
- **五种搜索模式** —— 从日常关键词到可编程脚本,一条搜索词直达目标(见下表)。
- **多搜索窗口** —— 一个进程多个窗口,每窗独立的视图、皮肤、缩放、搜索模式、列布局与全局热键。
- **插件系统** —— 清单 + 原生 DLL 双件式插件,菜单/搜索模式/右键菜单/预览/状态栏/事件订阅均可扩展,权限分级、启用需确认。
- **多语言界面** —— 简体中文、繁体中文、English、한국어、ไทย、Bahasa Melayu,可每窗独立设置。
- **预览面板** —— 图片、文本、Markdown、驱动器空间一览,选中即预览。
- **文件操作全套** —— 行内重命名(F2)、设置别名、剪贴板剪切/复制/粘贴、OLE 拖出文件、定位与打开。

## 📉 资源占用实测(内存与索引)

三款常驻搜索工具**同一台机器、同时运行、空闲稳态**下的实测(2026-09-21,索引规模约 **451 万** 文件条目;
版本:蜗牛快搜 v1.0.0、Everything 1.5(1.5.0.1423b)、Listary v7(7.0.0.9);
1 MB = 1048576 字节;连续 3 次采样、间隔 4 秒,数值波动 < 0.5 MB)。

### 常驻内存

| 指标(主进程) | 蜗牛快搜 | Everything | Listary |
|---|---|---|---|
| 专用工作集(≈任务管理器"内存"列) | **280.8 MB** | 377.1 MB | 395.1 MB |
| 提交大小(私有内存申请量) | **325.2 MB** | 386.2 MB | 544.0 MB |
| 工作集(含共享 DLL 的物理驻留) | **314.1 MB** | 410.3 MB | 528.5 MB |
| 线程数 / 句柄数 | 27 / 725 | 21 / 563 | 53 / 约 1370 |

三项内存指标蜗牛快搜均为最低:专用工作集较 Everything 低 **25.5%**、较 Listary 低 **28.9%**;提交大小分别低 15.8% / 40.2%;工作集分别低 23.4% / 40.6%。

进程构成上,蜗牛快搜是唯一单进程形态:

| 工具 | 进程构成 | 全部进程合计(专用工作集) |
|---|---|---|
| 蜗牛快搜 | 单进程 | **280.8 MB** |
| Everything | 主实例 + 后台辅助实例 | 379.0 MB |
| Listary | 主程序 + 后台服务 + 辅助进程 | 411.0 MB |

> 口径说明:
> 专用工作集 = 独占且当前驻留物理内存的部分,即任务管理器"内存"列;
> 提交大小 = 向系统申请的私有内存总量(含已换出到页面文件的冷数据,Listary 当时另有约 149 MB 已换出,峰值压力比表面数字更大);
> 工作集 = 物理驻留总量。

### 索引文件磁盘占用

| 工具 | 索引文件 | 磁盘占用 | 说明 |
|---|---|---|---|
| 蜗牛快搜 | `xjs_db.dat`(程序目录) | **143 MB**(150,110,481 字节) | 运行期增量维护,实测时点即现值 |
| Everything | `Everything.db`(安装目录) | 146 MB(152,799,695 字节) | 按其设置在空闲/退出时保存 |
| Listary | `disk-index.lydb`(用户配置目录) | 212 MB(222,207,324 字节) | 三者中最大 |

同样约 451 万条目,蜗牛快搜的磁盘库最小;常驻内存与磁盘库之比:Listary 1.86×、蜗牛快搜 1.96×、Everything 2.59×。

> 三者为不同软件,功能与架构不同,数字仅供量级参考;以上为空闲稳态实测,长时间重度使用后各家缓存都会增长。
> 评估报告出自`deepseek`实机测试,未曾人工干预

## 🔍 搜索模式

| 模式 | 搜索词示例 | 一句话定位 |
|---|---|---|
| 通配符(关键词) | `*.txt`、`文档`、`wd` | 日常默认:星号/问号 + 拼音首拼/全拼 + 且/或组合 |
| 正则 | `^[0-9]+$` | PCRE2 正则,精确控制匹配规则 |
| SQL 语句 | `SELECT Path FROM alltable WHERE Size > '100M'` | 类 PostgreSQL 复杂查询,功能最强 |
| Lua 过滤 | `f.ext() == "pdf"` | 逐文件谓词过滤,可编程的"是否命中" |
| Lua 执行 | 完整 Lua 程序 | 脚本完全接管搜索:自己遍历、自己排序 |

通配符模式内置**拼音搜索**(首拼 `wd`、全拼 `wendang` 均可命中"文档")、**多关键词组合**(空格=且,`|`=或)与**自动加星**,细节见 [搜索模式简介.md](搜索模式简介.md)。

## 🎨 界面与交互

- 纯 Direct2D 全自绘 UI(无一个原子控件),另提供 **GDI+ 兼容渲染引擎**可选,重启生效。
- 17 款皮肤主题一键切换;列表/详情两种视图,列可拖动换位、调宽、显隐,按字段持久化。
- 搜索框支持输入历史导航(Alt+←/→)、托管标签、搜索模式药丸菜单。
- 全局呼出:双击 Ctrl 唤起目标窗口、每窗可录制独立全局热键、托盘常驻。
- 每窗可配置:失焦行为、激活落点(跟随鼠标/九宫格落点)、置顶图钉、任务栏图标、单双击打开、默认选中首项等。
- 鼠标滚轮/Ctrl 组合:切换视图、缩放预览、页面缩放 50%~200%。

## 🧩 插件

插件为 `manifest.json` + DLL 双件结构,声明式能力(菜单、搜索模式)无需写代码,动态能力用纯 C ABI SDK 开发:

```text
plugins/<插件id>/
  manifest.json    ← 能力声明(中文主键)
  <插件>.dll        ← 可缺省;纯声明式插件可以没有 DLL
```

- 权限四档:`file.read` / `file.write` / `exec` / `ui`,声明在清单、边界强制、管理页可审计(不是沙箱)。
- 内置示例插件 `space-map`(驱动器空间地图),见 [插件示例/](插件示例/)。
- 开发入门见 [插件开发指南.md](插件开发指南.md)。

## 🖥️ 系统要求

- Windows 10(1809+)或 Windows 11,x64。
- 程序要求管理员权限运行(非管理员启动时会自动请求 UAC 重启),以保证全盘索引与操作权限。

## 📦 下载与运行

前往 [Releases](../../releases) 下载,解压后运行 `SnailQuickSearch.exe`:

```text
SnailQuickSearch.exe   ← 主程序
xunjieso.dll           ← 搜索引擎(随包分发)
languages\             ← 语言包
skin\                  ← 皮肤主题
```

首次启动会自动建立全盘索引,之后增量维护;关闭默认隐藏到托盘。

## 🔨 从源码构建

- **工具链**:Visual Studio 2022(MSVC v143,17.14 实测)+ Windows SDK,编译标准 **C++20**。
- **引擎 SDK**:`xunjieso.h` / `xunjieso.lib` 已随仓库提供;运行所需的 `xunjieso.dll` 请从 Releases 获取,放到仓库根目录。
- **构建**:双击 `build.bat` 即可——每次全量编译(rc 资源 → 全部源码并行 → 链接),产物 `SnailQuickSearch.exe` 输出到仓库根目录。

```bat
git clone <本仓库>
build.bat
```

插件示例的构建同样是一键:`插件示例/space-map/build.bat`。

## 📄 授权

蜗牛快搜为**免费软件**:任何人可在个人或商业环境中免费使用,成品层源码开放查看、修改与二次开发。随附的 xunjieso 搜索引擎为版权人自研的闭源组件,随本软件免费授权使用、集成与分发,不含源码。

修改版与再分发须遵守授权声明(包括保留产品名称、品牌标识与赞助二维码,修改版不得冒用"官方"等表述)。完整条款见 [THIRD-PARTY-NOTICES.md](THIRD-PARTY-NOTICES.md)。

## ☕ 赞助

如果蜗牛快搜帮到了你,欢迎请作者喝杯咖啡:

| 微信 | 支付宝 |
|---|---|
| ![微信赞助](sponsor-wechat.png) | ![支付宝赞助](sponsor-alipay.jpg) |
