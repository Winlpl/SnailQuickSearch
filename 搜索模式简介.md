# 蜗牛快搜 搜索模式简介

蜗牛快搜提供 `5种` 搜索模式

| 模式 | 搜索词示例 | 一句话定位 |
|---|---|---|
| 通配符(关键词) | `*.txt`、`文档` | 日常默认:星号/问号 + 拼音 + 且/或组合 |
| 正则 | `^[0-9]+$` | PCRE2 正则,精确控制匹配规则 |
| SQL 语句 | `SELECT Path FROM alltable WHERE Size > '100M'` | 类 PostgreSQL 复杂查询,功能最强 |
| Lua 过滤 | `f.ext() == "pdf"` | 逐文件谓词过滤,可编程的"是否命中" |
| Lua 执行 | 完整 Lua 程序(以执行类型提交,可存为自定义模式) | 脚本完全接管搜索:自己遍历、自己排序 |

---

## 1. 通配符——日常默认模式

最常用的关键词模式,搜索词同时支持通配符、拼音和多关键词组合。

**通配符**
- `*` 匹配任意长度字符,`?` 匹配单个字符(可用设置开关)。
- **自动加星**:搜索词不含 `*` 或 `?` 时,前后自动补 `*`(即按"包含"匹配);写 `*.exe` 则按通配符原义匹配。
- **文件名 / 路径**:搜索词含 `\`(写 `/` 会自动替换成 `\`)时按完整路径匹配,否则只匹配文件名。

**拼音**(通配符模式的内置能力,无需切换)
- **首拼**:`wd` 命中 `文档.docx`;**全拼**:`wendang` 同样命中。
- 由三个设置控制:`支持首拼`(默认开)、`支持全拼`(默认开)、`拼音完整匹配`(默认关)。
- 高亮与拼音同源:拼音命中的汉字同样会高亮。

**多关键词组合**
- **且**:用空格分隔,如 `报表 2026`(两个词都要命中)。
- **或**:用 `|` 分隔,如 `*.doc | *.docx`(任一命中即可)。且/或符号可在设置里改。

**其他行为**
- 默认不区分大小写(可在设置开启);自动删除搜索词首尾空格。
- 搜索词为空时默认显示全部文件(可在设置关闭)。

## 2. 正则

基于 **PCRE2**(UTF 模式 + JIT 加速)的正则匹配,适合通配符表达不了的精确规则。

```
^[0-9]{4}-报告.*\.docx$
```

- 大小写跟随全局"区分大小写"设置(默认不区分,即自动加 `忽略大小写` 选项)。
- 正则中出现 `\\`(双反斜杠)视为路径分隔符,整条正则改为对**完整路径**匹配;单个 `\` 是正则转义,不受影响。
- 表达式编译失败时触发**搜索失败**事件,错误 JSON 含 `错误类型="正则错误"`、`错误信息`、`出错位置`。

## 3. SQL 语句——功能最强

类 PostgreSQL 语法(libpg_query 解析),对单表 `alltable` 做复杂查询。指定类型 2 后**100% 按 SQL 解析**(不以 `SELECT`/`WITH` 开头也强制走 SQL)。

```sql
SELECT Path FROM alltable
WHERE FileType = '视频' AND Size > '100M' AND ModTime > NOW() - INTERVAL '7 days'
ORDER BY Size DESC LIMIT 100;
```

**主要支持**
- 基础:`SELECT/DISTINCT/WHERE/ORDER BY/LIMIT/OFFSET`(FROM 仅 `alltable`)。
- 模式匹配:`LIKE / NOT LIKE / ILIKE`(`%`、`_`、`\` 转义),正则运算符 `~ ~* !~ !~*`(PCRE2 JIT)。
- 聚合:`GROUP BY / HAVING / COUNT / SUM / AVG / MIN / MAX / COUNT(DISTINCT)`。
- 表达式:`LOWER/UPPER/LENGTH/SUBSTRING/MD5/CAST/COALESCE/NULLIF/CASE WHEN`;`MD5(FileContent)` 自动走流式内容搜索(读取上限 200MB)。
- `WITH`(CTE)、`alltable` 自连接 `INNER JOIN`、集合操作 `UNION [ALL] / INTERSECT [ALL] / EXCEPT [ALL]`(实验性)。
- 尺寸/时间简写:`'100M' '2G' '500K'` 自动转字节;`NOW() / CURRENT_DATE / INTERVAL 'N days'` 编译期求值。

**常用字段**:`ID、ParentID、Path、FName、Ext、ParentName、ParentPath、AnyParent、Size、CreateTime、ModTime、AccessTime、FileType、IsDir、Alias、Score、FAttr、FileContent`(列名不区分大小写)。

**不支持**:窗口函数(OVER)、写操作/DDL、多表 FROM、EXISTS、VALUES、多语句、`::` 类型转换等

**注意**
- LIKE 模式中反斜杠必须写 `\\`;字符串比较默认区分大小写,不区分用 `ILIKE`。
- 带 `ORDER BY` 或聚合的查询结果为"乱序"结果集:删除会同步移出,新建/修改不同步(重新搜索即刷新)。


## 4. Lua 过滤——逐文件判断

引擎内嵌 **Lua 5.5.1**(支持中文标识符扩展)。**每个文件一条线程,同一脚本对所有文件并发求值**(一个文件一条线程同时执行),真=命中;输出保持查询顺序,新建/删除/重命名**全部支持增量同步**,也可作为多重搜索的阶段。

**表达式模式**——一个表达式就是一个过滤器:

```lua
f.ext() == "pdf" and f.size() > 1048576
```

**高级模式**——首个有效行以 `return function` 开头即整段原样编译,可写预处理、循环、聚合:

```lua
local 后缀集 = { pdf = true, docx = true }
return function(f)
    return 后缀集[f.ext()] ~= nil
end
```

- `f` 表是当前文件的只读视图,字段惰性求值(没调到就不算):`f.fname()/f.ext()/f.fpath()/f.size()/f.mtime()/f.isdir()/f.filetype()/f.rating()/f.alias()/f.anyparentname()` 等,链式阶段里还有 `f.index()/f.total()` 可做 TOP-N、抽样。
- **脚本必须无状态**:每文件一条线程并发跑,文件间没有固定先后顺序,预处理段(`return function` 之前的代码)也是各线程各自执行、互不共享——不要在脚本里计数或累积跨文件的全局状态;要跨文件聚合/统计/排序请用 Lua 执行模式(`db`/`res` 表)。
- 沙箱:删除 `io/os/package/debug/coroutine/load/require` 等危险库,一切 API 只读;脚本 ≤ 64KB,单 VM 内存 256MB,单文件求值超时 50ms(超时视为不匹配)。


## 5. Lua 执行——脚本接管搜索

以**执行类型**(`XJS_KEYWORD_LUA_EXEC`,经 `xjs_result_Query` 提交)运行:脚本自己遍历数据库、自己筛选排序,`return` 的 ID 数组(及顺序)就是最终结果。

```lua
local 结果 = {}
for id in db.ids() do
    local f = db.get(id)
    if f and f.ext() == "md" then 结果[#结果+1] = id end
end
table.sort(结果)   -- 想怎么排就怎么排
return 结果
```

- 可见三张表:`f`(文件字段)+ `db`(遍历:`db.count/db.ids/db.files/db.get`)+ `res`(当前结果只读快照:`res.count/res.get/res.ids`)。
- 全程持引擎读锁,看门狗总预算 10 秒;结果为脚本自定义序:删除会同步移出,新建/修改/重命名不同步(重新执行即刷新)。
- 与 -3 的区别:-3 是"每个文件问一次脚本"(可进多重搜索链、可增量同步);-4 是"脚本就是整个搜索程序"(一期不进多重搜索链)。
- **可保存为自定义搜索模式**(模式菜单 → ✎ 添加,类型选"Lua 执行"):输入模式名命中即单发执行,模板 `<keyword>` 占位符吃掉搜索框输入词;不与其它模式组链。

---

## 附:如何选择

| 需求 | 用哪个 |
|---|---|
| 日常找文件、拼音缩写 | 通配符 |
| 文件名格式精确匹配 | 正则 |
| 按大小/时间/类型/属性组合筛选、统计聚合 | SQL |
| 自定义命中逻辑(逐文件判断) | Lua 过滤 |
| 自定义遍历+排序,完全接管结果 | Lua 执行 |
